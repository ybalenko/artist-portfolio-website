# Public browser configuration for the Contacts Leave a message form.
# This value is safe to expose because it is sent to the visitor's browser.
# Do not put private recipient emails, SES secrets, AWS keys, or other secrets here.

PUBLIC_CONTACT_API_URL=
