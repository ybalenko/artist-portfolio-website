---
import { exhibitionSections } from "../../data/exhibitions";

export function getStaticPaths() {
  return exhibitionSections.map((section) => ({
    params: { status: section.status },
  }));
}

return Astro.redirect("/exhibitions/");
---
